#include <iostream>

int main(int argc, char const *argv[]) {
  std::cout << "Variable names in C++ can only start with letters or underscores and can only contain letters, numbers, and underscores" << std::endl;
  std::cout << "Mulit-assignment works: int potato, numberOfDragons, unicorn_poop; " << std::endl;
  std::cout << "The above creates 3 ints" << std::endl;
  std::cout << "Variables can be declared without giving them a value" << std::endl;
  std::cout << "" << std::endl;

  return 0;
}
